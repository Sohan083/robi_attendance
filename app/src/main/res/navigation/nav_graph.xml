<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nav_graph"
    app:startDestination="@id/attendance_nav_graph">
    <include
        app:graph="@navigation/logout_nav_graph"/>
    <include
        app:graph="@navigation/leave_nav_graph"/>
    <include
        app:graph="@navigation/attendance_nav_graph"/>
    <include
        app:graph="@navigation/attendance_summary_nav_graph"/>
</navigation>